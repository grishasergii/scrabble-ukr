const getWordAtAnchor = (squares, rack, dictionary, anchor, stepVertical) => {
  // if (anchors.length === 0) {
  //   return null;
  // }

  // const anchorIndex = anchors[Math.floor(Math.random() * anchors.length)];

  

};

export default getWord;